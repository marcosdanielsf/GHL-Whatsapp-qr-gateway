# Port donde corre la API
PORT=8080

# Redis (ejemplo docker-compose)
REDIS_URL=redis://redis:6379

# Carpeta donde se persisten las sesiones de Baileys
SESSION_DIR=./data/sessions

# Delays (ms)
TEXT_DELAY_MS=3500
MEDIA_DELAY_MS_MIN=6000
MEDIA_DELAY_MS_MAX=9000

# Orígenes permitidos para el frontend (separa por comas)
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Endpoint donde el gateway enviará INBOUND messages a GHL
# Reemplaza por tu URL de GHL o por un ngrok mientras desarrollas
GHL_INBOUND_URL=https://your-ghl-inbound.example/webhook

# Base de ngrok (opcional, útil en desarrollo)
NGROK_BASE_URL=https://xxxxxx.ngrok-free.app

# Opcional: webhook/monitoring
MONITORING_WEBHOOK_URL=
MONITORING_WEBHOOK_TOKEN=

# En producción, añade seguridad: API keys, secretos, TLS
# NO subas .env con credenciales reales al repositorio público.
